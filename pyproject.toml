[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "patri"
version="1.0.0"

# [project.scripts]
# patricomando = "patri.__main__:main"